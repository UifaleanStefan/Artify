# ── Required for production ─────────────────────────────────
# Replicate API (get token at replicate.com)
REPLICATE_API_TOKEN=r8_your_token_here

# PostgreSQL
DATABASE_URL=postgresql://user:password@host:5432/artify_db

# Must be your live HTTPS URL (used for image URLs, emails, Replicate callbacks)
PUBLIC_BASE_URL=https://your-domain.com

# Email – at least one required for result/failure emails (Resend recommended)
RESEND_API_KEY=re_your_key_here
# SENDGRID_API_KEY=
# SMTP_HOST=
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASSWORD=

# FROM_EMAIL=noreply@yourdomain.com
# FROM_NAME=Artify

# ── Optional ────────────────────────────────────────────────
# Upload directory (default: temp; use persistent path on server)
# UPLOAD_DIR=/var/lib/artify/uploads
# Result image TTL in DB (days)
# RESULT_IMAGE_TTL_DAYS=14
